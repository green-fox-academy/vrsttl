'use strict';
export { };

let animals = ['kuty', 'macsk', 'cic'];
animals = animals.map(function (addA) {return addA + 'a'});
console.log(animals);

// -  Create an array variable named `animals`
//    with the following content: `["kuty", "macsk", "cic"]`
// -  Add all elements an `"a"` at the end